<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>QuotingDojo</title>
    <link rel="stylesheet" href="/style/quotes.css">
  </head>
  <body>
    <div class="container">
      <h2>Here are the awesome quotes!</h2>
      <% if(all_quotes) { %>
        <% for(let x in all_quotes) { %>
          <p><%= all_quotes[x].name %></p>
          <blockquote cite="http://">
            --- <%= all_quotes[x].quote %> at <%= all_quotes[x].createdAt %>
          </blockquote>
          <h4>Likes: <%= all_quotes[x].likes %></h4>
          <form class="" action="/quotes/like/<%= all_quotes[x]._id%>" method="post">
            <input type="submit" name="" value="Like">
          </form>
          <hr>
        <% } %>
      <% } %>
      <a href="/">Add a quote</a>
    </div><!-- end_of_container -->
  </body>
</html>
